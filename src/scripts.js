
import getApiData from './api';
import './css/base.scss';
import User from './Customer';

// function getData()

function getId(){
  //query selector for input
  //grab input with .value
  // take value and store in a variable
  // separate name from id
  // return id
}


function isManager(id){
  // if id === undefined
  // return true;
}

function findUserById(id) {
// compare passed id to users id
// iterate over users array with find
// return that user
}

handleSubmit() {
  id = getId()
  if(!isManager()) {
    invoke manager function
  } else {
  user = findUserById()
  let loggedInUser = new User()
  render page functoin
  }
}